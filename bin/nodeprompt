#!/usr/bin/env node

/**
 * The main program to be executed to generate PS1 (bash).
 *
 * @license MIT
 */

/* global process, __dirname */

'use strict';

const Nodeprompt = require( __dirname + '/../lib/nodeprompt.js' );
const prompt = new Nodeprompt().print();

process.stdout.write( prompt );
